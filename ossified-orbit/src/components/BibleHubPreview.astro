
<section class="bible-hub-preview">
  <div class="container">

    <div class="preview-grid">

      <div class="preview-content">

        <span class="section-tag">
          📖 New
        </span>

        <h2>
          Grow deeper in God's Word every day.
        </h2>

        <p class="lead">
  Read, search and study God's Word with confidence. Whether you're
  following a Bible reading plan, preparing for Bible study, or
  exploring a passage during prayer, Bible Hub brings Scripture and
  trusted study tools together in one beautiful experience.
</p>

        <ul class="feature-list">
  <li>Read every book and chapter</li>
  <li>Search Scripture instantly</li>
  <li>Navigate books with ease</li>
  <li>Study in a distraction-free experience</li>
</ul>

        <a href="/bible" class="btn-primary">
          Explore Bible Hub
        </a>

      </div>

      <div class="preview-image">

        <img
          src="/images/bible-hub-preview.webp"
          alt="Bible Hub preview showing Scripture reading and search"
          loading="lazy"
          decoding="async"
        />

      </div>

    </div>

  </div>
</section>

<style>
.bible-hub-preview {
  padding: 3rem 0 5rem;
}

.preview-grid {
  display: grid;
  grid-template-columns: 45% 55%;
  gap: 4rem;
  align-items: center;
}

.preview-content h2 {
  font-size: clamp(2rem, 4vw, 2.8rem);
  line-height: 1.2;
  margin: 0.75rem 0 1rem;
  color: var(--color-heading);
}

.preview-content .lead {
  margin-bottom: 1.75rem;
  color: var(--color-text);
  line-height: 1.8;
}

.feature-list {
  list-style: none;
  padding: 0;
  margin: 0 0 2rem;
  display: grid;
  gap: 0.9rem;
}

.feature-list li {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  color: var(--color-text);
  line-height: 1.6;
}

.feature-list li::before {
  content: "✓";
  color: var(--color-primary);
  font-weight: 700;
  flex-shrink: 0;
}

.preview-image img {
  width: 100%;
  display: block;
  border-radius: 1.25rem;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.12);
}

@media (max-width: 900px) {
  .bible-hub-preview {
    padding: 3rem 0;
  }

  .preview-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  .preview-content {
    order: 2;
  }

  .preview-image {
    order: 1;
  }
}
</style>